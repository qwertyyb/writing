import KoaRouter from '@koa/router';

export default new KoaRouter({ prefix: '/article' })
  .get('list', (req, next) => {

  })
  .patch('update', (req, next) => {

  })
  .del('remove', (req, next) => {

  })
  .post('add', (req, next) => {

  })
